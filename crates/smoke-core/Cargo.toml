[package]
name = "smoke-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rand = "0.9"
rand_chacha = "0.9"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
