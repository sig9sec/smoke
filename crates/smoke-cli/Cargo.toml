[package]
name = "smoke-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "smoke"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
smoke-core = { path = "../smoke-core" }
serde_json = "1"
